<Page x:Class="_222_Katakhova.CategoryTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_222_Katakhova"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Категории">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10" BorderThickness="0 0 0 1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="SearchTextBox" Grid.Column="1" 
                         TextChanged="SearchTextBox_TextChanged"
                         Padding="5" VerticalContentAlignment="Center"
                         ToolTip="Введите текст для поиска по названию категории"/>

                <Button x:Name="ClearSearchButton" Grid.Column="2" 
                        Content="Очистить" Margin="10,0,0,0"
                        Click="ClearSearchButton_Click"
                        ToolTip="Очистить поиск"/>

                <Button x:Name="SortButton" Grid.Column="3" 
                        Content="А-Я" Margin="10,0,0,0"
                        Click="SortButton_Click"
                        ToolTip="Сортировать от А до Я"
                        Padding="10,5"/>
            </Grid>
        </Border>

        <TextBlock x:Name="ResultsInfoText" Grid.Row="1" 
                   Margin="10,5" FontSize="12"/>

        <DataGrid Grid.Row="2" x:Name="DataGridCategory" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название категории" Binding="{Binding Name}" Width="*"/>
                <DataGridTemplateColumn Width="120" Header="Действия">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="ButtonEdit" Content="Редактировать" 
                                    ToolTip="Редактировать категорию" 
                                    Click="ButtonEdit_Click"
                                    Padding="5,2" FontSize="10"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="ButtonAdd" ToolTip="Добавить категорию" 
                        Content="Добавить категорию" HorizontalAlignment="Left" 
                        Click="ButtonAdd_Click" Padding="10,5" MinWidth="120"/>

                <Button Grid.Column="1" x:Name="ButtonDel" ToolTip="Удалить категории" 
                        Content="Удалить выбранные" HorizontalAlignment="Right" 
                        Click="ButtonDel_Click" Padding="10,5" MinWidth="120"/>
            </Grid>
        </Border>
    </Grid>
</Page>